extends layout

block content 
    h2 You are currently viewing our #{name} cars

    if brands_list
        each car in brands_list
            a(href=brand.url)

    a(href=`/${name}/create`).create Click here to add a new car to this brand
    a(href=`${name}/delete`).delete Click here to delete this brand